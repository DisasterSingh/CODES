def revarr(arr):
    n = len(arr)
    start= 0
    end = n-1
    temp = 0
    while start < end:
        temp = arr[start]
        arr[start] = arr[end]
        arr[end] = temp
        start = start+1
        end = end-1
    print(arr)
arr = [1,2,3,4,5]
revarr(arr)
        
