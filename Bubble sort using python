bubble sort using python 
(repeatedly swapping of elements )

def arrbubble(arr,n):
    for i in range(0,n):
        for j in range(0,n-1):
            if arr[j]>arr[j+1]:
                temp = arr[j]
                arr[j] = arr[j+1]
                arr[j+1]=temp

    print(arr)
arr= [67,29,11,12,91,1,28,78]
n = len(arr)
arrbubble(arr,n)
